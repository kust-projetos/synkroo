# The database has no host port. Traffic reaches it only through cloudflared
# on this compose network and must be encrypted and SCRAM-authenticated.
local   all             all                                     trust
hostssl all             all             0.0.0.0/0               scram-sha-256
hostssl all             all             ::/0                    scram-sha-256
