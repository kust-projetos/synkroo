---
phase: "05"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/appointments/[id]/cancel/route.ts
  - src/components/calendar/AppointmentDialog.tsx
  - src/components/calendar/store/calendar-store.ts
  - src/components/contacts/contact-appointments-tab.tsx
  - src/app/api/contacts/[id]/appointments/route.ts
autonomous: true
requirements:
  - CAL-01
  - CAL-02
  - CAL-03
  - CAL-04

must_haves:
  truths:
    - "User can book appointment from contact profile with patient data pre-filled"
    - "User can book appointment from lead card with lead data pre-filled"
    - "Calendar events are auto-linked to patient contacts via patient_id FK"
    - "User can view patient upcoming and past appointments in contact profile"
  artifacts:
    - path: "src/components/calendar/store/calendar-store.ts"
      provides: "dialog state with defaultPatientId/defaultLeadName pre-fill fields"
    - path: "src/components/calendar/AppointmentDialog.tsx"
      provides: "pre-fills patient/lead data when dialog opens with pre-fill context"
    - path: "src/components/contacts/contact-appointments-tab.tsx"
      provides: "tab showing upcoming/past appointments per contact"
    - path: "src/app/api/contacts/[id]/appointments/route.ts"
      provides: "appointments list filtered by patient_id"
    - path: "src/app/api/appointments/[id]/cancel/route.ts"
      provides: "calls processWaitlistOnCancellation on cancel"
  key_links:
    - from: "AppointmentDialog.tsx"
      to: "calendar-store.ts"
      via: "dialog state defaultPatientId"
    - from: "contact-appointments-tab.tsx"
      to: "/api/contacts/[id]/appointments"
      via: "fetch appointments by patient"
    - from: "cancel route"
      to: "waitlist.service.ts"
      via: "processWaitlistOnCancellation call"
---

<objective>
Calendar-CRM bidirectional linking: book appointments from contact/lead context, auto-link appointments to patients, view appointment history in contact profile, connect waitlist auto-fill to appointment cancellation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/calendar/AppointmentDialog.tsx
@src/components/calendar/store/calendar-store.ts
@src/services/waitlist/waitlist.service.ts
@src/app/api/reports/export/route.ts
@.planning/phases/05-integration-analytics/05-RESEARCH.md
@.planning/phases/05-integration-analytics/05-UI-SPEC.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/calendar/store/calendar-store.ts:
```typescript
interface CalendarDialogState {
  open: boolean
  mode: 'create' | 'edit'
  slotInfo?: {
    date: Date
    hour: number
    minute: number
    dentistId?: string
  }
  // NEW: pre-fill fields for contact/lead booking
  defaultPatientId?: string
  defaultLeadName?: string
  defaultLeadPhone?: string
}
```

From src/services/waitlist/waitlist.service.ts:
```typescript
export async function processWaitlistOnCancellation(
  clinicId: string,
  date: string,
  time: string,
  dentistId?: string
): Promise<{ notified: number }>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend calendar-store with pre-fill state</name>
  <files>src/components/calendar/store/calendar-store.ts</files>
  <read_first>src/components/calendar/store/calendar-store.ts</read_first>
  <action>
Add three optional fields to the CalendarDialogState interface in calendar-store.ts:
- `defaultPatientId?: string` — patient ID to pre-fill
- `defaultLeadName?: string` — lead name to pre-fill  
- `defaultLeadPhone?: string` — lead phone to pre-fill

Add three new actions to the store:
- `prefillFromPatient(patientId: string)` — sets mode to 'create', opens dialog, sets defaultPatientId
- `prefillFromLead(name: string, phone: string)` — sets mode to 'create', opens dialog, sets defaultLeadName/defaultLeadPhone
- `clearPrefill()` — clears all three pre-fill fields

The store already has `openDialog` and `closeDialog` actions — use those for prefill actions.
  </action>
  <verify>
<automated>grep -n "defaultPatientId\|defaultLeadName\|defaultLeadPhone\|prefillFromPatient\|prefillFromLead" src/components/calendar/store/calendar-store.ts</automated>
  </verify>
  <done>Calendar store has pre-fill state fields and actions for booking from contact/lead</done>
</task>

<task type="auto">
  <name>Task 2: Update AppointmentDialog to pre-fill from store state</name>
  <files>src/components/calendar/AppointmentDialog.tsx</files>
  <read_first>src/components/calendar/AppointmentDialog.tsx</read_first>
  <action>
In the AppointmentDialog component, add a new useEffect that runs when dialog.open becomes true AND dialog.mode === 'create' AND (dialog.defaultPatientId OR dialog.defaultLeadName) is set:

**For defaultPatientId:**
1. Fetch patient from /api/patients/[id] using createTypedClient()
2. Set form.patientName to patient.name
3. Set form.patientPhone to patient.phone

**For defaultLeadName (if defaultPatientId not set):**
1. Set form.patientName to dialog.defaultLeadName
2. Set form.patientPhone to dialog.defaultLeadPhone

After pre-filling, call store.clearPrefill() to reset the pre-fill state.

Existing slotInfo pre-fill logic (lines 59-73) should remain unchanged — pre-fill from contact takes precedence when both are available.
  </action>
  <verify>
<automated>grep -n "defaultPatientId\|defaultLeadName\|clearPrefill" src/components/calendar/AppointmentDialog.tsx</automated>
  </verify>
  <done>AppointmentDialog pre-fills patient/lead data when opened from contact/lead context</done>
</task>

<task type="auto">
  <name>Task 3: Create appointments API route per contact</name>
  <files>src/app/api/contacts/[id]/appointments/route.ts</files>
  <read_first>src/lib/supabase/typed.ts</read_first>
  <action>
Create route file: src/app/api/contacts/[id]/appointments/route.ts

GET handler accepting contact/patient ID from URL param:
1. Authenticate via validateApiAuth() — return 401 if failed
2. Query appointments table with patient_id = param.id using createTypedClient()
3. JOIN with patients(name), dentists(name), procedures(name) using Supabase relationship syntax
4. Order by scheduled_at DESC
5. Return { appointments: [...] }

Query should look like:
```typescript
supabase
  .from('appointments')
  .select(`id, scheduled_at, status, notes, duration_minutes, patients(name), dentists(name), procedures(name)`)
  .eq('patient_id', params.id)
  .order('scheduled_at', { ascending: false })
```

No changes needed to existing appointments table — patient_id FK already exists (CAL-03).
  </action>
  <verify>
<automated>grep -l "contacts.*appointments" src/app/api/contacts/ -r 2>/dev/null && echo "route exists" || echo "route missing"</automated>
  </verify>
  <done>API route returns appointments filtered by patient ID for contact profile view</done>
</task>

<task type="auto">
  <name>Task 4: Create contact appointments tab UI</name>
  <files>src/components/contacts/contact-appointments-tab.tsx</files>
  <read_first>src/components/contacts/contact-timeline-tab.tsx</read_first>
  <action>
Create new file: src/components/contacts/contact-appointments-tab.tsx

Follow the ContactTimelineTab pattern from src/components/contacts/contact-timeline-tab.tsx:

1. **Tab structure:** Two sub-tabs via Tabs component — "Proximos" | "Anteriores"
2. **Fetching:** useQuery to call GET /api/contacts/[contactId]/appointments on mount
3. **"Proximos" filter:** Filter appointments where scheduled_at > now, sort ASC
4. **"Anteriores" filter:** Filter appointments where scheduled_at <= now, sort DESC
5. **Appointment cards:** Compact cards showing date/time, procedure, dentist, status badge
6. **Status badges:** Confirmado=green, Cancelado=red, Realizado=blue, Remarcado=amber (per UI-SPEC)
7. **CTA:** "Novo Agendamento" button at top-right using primary accent color (blue-600)

Status badge colors per UI-SPEC:
- Confirmado: green
- Cancelado: red  
- Realizado: blue
- Remarcado: amber

Empty state for contact with no appointments: "Nenhum agendamento" heading + body "Clique em Novo Agendamento para criar um agendamento para este contato."
  </action>
  <verify>
<automated>ls src/components/contacts/contact-appointments-tab.tsx && grep -n "Proximos\|Anteriores\|Novo Agendamento" src/components/contacts/contact-appointments-tab.tsx</automated>
  </verify>
  <done>Contact appointments tab shows upcoming/past appointments with status badges and booking CTA</done>
</task>

<task type="auto">
  <name>Task 5: Wire appointment cancel to waitlist auto-fill</name>
  <files>src/app/api/appointments/[id]/cancel/route.ts</files>
  <read_first>src/app/api/appointments/[id]/cancel/route.ts</read_first>
  <action>
Read the existing cancel route file. After the appointment cancellation is successful, add a call to:

```typescript
import { processWaitlistOnCancellation } from '@/services/waitlist/waitlist.service'
```

Extract date, time, and dentistId from the cancelled appointment, then call:
```typescript
await processWaitlistOnCancellation(
  clinicId,
  cancelledAppointment.scheduled_at.split('T')[0], // date part
  cancelledAppointment.scheduled_at.split('T')[1].substring(0, 5), // time part HH:MM
  cancelledAppointment.dentist_id
)
```

The processWaitlistOnCancellation function (from waitlist.service.ts line 459) finds matching waitlist entries and notifies the first patient via WhatsApp.

No new waitlist service code needed — just wire the existing function into the cancel flow.
  </action>
  <verify>
<automated>grep -n "processWaitlistOnCancellation" src/app/api/appointments/[id]/cancel/route.ts</automated>
  </verify>
  <done>Cancelled appointments trigger waitlist auto-fill notification to matching patients</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> AppointmentDialog | User clicks "book" on contact/lead, dialog opens with pre-filled data |
| API -> appointments table | RLS enforced via createTypedClient() — clinic_id boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | AppointmentDialog pre-fill | mitigate | Validate pre-fill data matches authenticated user's clinic via RLS |
| T-05-02 | Denial | Contact appointments tab | accept | Read-only query, low risk |
| T-05-03 | Information Disclosure | Waitlist WhatsApp notify | mitigate | WhatsApp notifications use clinic-scoped phone numbers only |
</threat_model>

<verification>
- AppointmentDialog opens pre-filled when opened from contact profile via prefilled state
- Appointments tab shows correct list filtered by contact
- Cancelled appointment triggers waitlist notification via processWaitlistOnCancellation
- All RLS-enforced via createTypedClient()
</verification>

<success_criteria>
1. User can click "Novo Agendamento" on contact profile → dialog pre-filled with patient name+phone
2. User can click "Agendar" on lead card → dialog pre-filled with lead name+phone
3. Appointments tab in contact profile shows upcoming and past appointments
4. Cancelled appointment auto-notifies matching waitlist patient
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-analytics/05-01-SUMMARY.md`
</output>
