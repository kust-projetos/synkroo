# F3.14 Coverage 63.11% → 70% — Audit 2026-08-26 (T1 consolidado)

> Gate W3: `jest.config.js:41` thresholds 70% branches/functions/lines/statements. `src/__tests__/coverage-boost.test.ts` estendido TDD para fechar gap sem inflar mock trivial. **T1 2026-08-26: coverage-boost 13→19 tests + campaign-branch 3 tests (F7.06) + clinics 3 tests = 69/69 targeted PASS; global 63.26% medido com full `--coverage` 286 suites.**

## Baseline 2026-08-24
- Statements 63.11% (10263/16262) FAIL threshold
- Lines 64.19% (9524/14836)
- Branches 48.74% Functions 55.20%
- `coverage/lcov.info` ausente (artefato gerado via `npm test -- --runInBand --coverage`)

## Full run 2026-08-26 — `npx jest --runInBand --coverage` 245.3s 286 suites 2067 passed
- **Statements 63.26% FAIL** (vs 63.11% baseline, +0.15pp)
- **Lines 64.36% FAIL** (vs 64.19%)
- **Branches 49.05% FAIL** (vs 48.74%)
- **Functions 55.41% FAIL** (vs 55.20%)
- Artefato: `coverage/lcov.info` 405089 bytes 2026-08-26 14:29:59 gerado fresh; `coverage/lcov-report/index.html` gerado.
- Comando: `npx jest --runInBand --coverage 2>&1 | Out-String` — timeout 295s ok Windows.

## Top 10 deltas (loc vs uncovered) — anotado via `coverage/lcov-report/index.html` 2026-08-26

| File | %Stmts | Uncovered Lines | Nota |
|------|--------|-----------------|------|
| `src/repositories/budgets/index.ts` | 0% | 6-521 (521 lines) | integração Drizzle; `npm run test:integration:run` |
| `src/repositories/treatment-plans/index.ts` | 0% | 6-406 (401 lines) | integração |
| `src/repositories/waitlist/index.ts` | 0% | 6-439 (434 lines) | integração |
| `src/repositories/appointments/index.ts` | 13.76% | 12-62,144-184,238-600 | parcial |
| `src/repositories/campaigns/index.ts` | 26.80% | 73-466 | partial |
| `src/services/followup/inactive-patient.service.ts` | 18.09% | 62-291 (pure helpers cobertos via `inactive-patient.test.ts` 4 tests; restante exige DB) | **fechado parcial 2026-08-25** |
| `src/services/campaigns/campaign.service.ts` (≡ `followup/campaign.service.ts`) | 59.09% | 60,93-108,115-122,144,158-169,185-249 | **F7.06 fechado 2026-08-26**: `sent===0 → failed` implementado + `campaign-branch.test.ts` 3 tests (100% falha → failed, opt-out → failed, partial → success) + `campaign-execution.test.ts` ajustado |
| `src/repositories/patients/index.ts` | 30.30% | 51-396 | partial |
| `src/services/followup/followup.service.ts` | 47.71% | 243-510 | |
| `src/lib/idempotency/index.ts` | 0% | 10-135 | |

Nota `src/repositories/clinics` **100%** via `clinics.test.ts` 3 tests (found/null/delegate) — prova padrão repo; demais repos 0% puxam global -6.7pp.

## Delta 2026-08-26 — extensão coverage-boost + campaign-branch

- `src/__tests__/coverage-boost.test.ts` 13→19 tests (antes 9):
  - **Novos 6 suites (2026-08-26)** — TDD sem mock trivial inflado:
    - `validation schemas — additional branches (dentist/message/budget/campaign)` — dentist phone/email invalid, message bad phone/empty, budget discount_percent 101, campaign invalid enum/empty template → cobre `src/lib/validations/dentist.ts`, `message.ts`, `budget.ts`, `campaign.ts` 100% statements (ver tabela: validations 81.81%→ todos 100% exceto index.ts)
    - `validation helpers — CPF check digits + custom messages + requireFields multi` — CPF 529.982.247-24 (check digit errado), custom message passthrough, validateFields allErrors 2, requireFields missing multi
    - `handleApiError — AppError branches (NotFound/Forbidden/RateLimit/External)` — 404/403/429/502/500 + toJSON details → `src/lib/errors.ts` 100% stmts (60,104 restantes são details)
    - `withRetry — non-Error throw + isRetryable null + custom retryableErrors` — string throw, MY_RETRY custom filter retry vs OTHER non-retryable → `src/lib/retry.ts` 96.42% stmts (vs ~30% antes)
    - `CircuitBreaker — half-open failure re-opens` — threshold 1, open→half-open fail re-open → complementa closed→open→half-open→closed já existente
    - `logger — level filtering and sensitive keys variants` — level warn filtra debug/info, connectionString redaction, array redaction, child inherits → `src/lib/logger.ts` 69.56% (vs 35% antes)
  - Mantém: `cn`, `validation` 8 branches, `timezone` 90.47%, `validations/*` 8 schemas, `errors` hierarchy, `withRetry`/`CircuitBreaker`/`tryCatch`/`handleApiError`/`createLogger` originais.

- `src/services/followup/campaign.service.ts:173-176` **fix F7.06**: `if (sent===0) await updateCampaignStatus(campaignId,'failed')` antes de return — antes retornava `No recipients delivered` sem persistir `failed`. `src/services/followup/__tests__/campaign-execution.test.ts` ajustado para `expect(...).toHaveBeenCalledWith(campaign.id,'failed')`.

- `src/services/campaigns/__tests__/campaign-branch.test.ts` **novo 3 tests** (T1 Step 3): 100% falha (0 sent) → failed + running, all opted-out → failed, partial success → success not failed — prova branch `sent===0` 100% coverage de campaigns delta.

- `src/repositories/clinics/__tests__/clinics.test.ts` 3/3 PASS (já existente) — garante repo pattern roda; verificado `npx jest ...clinics.test.ts --runInBand` PASS.

- Verificação targeted 2026-08-26: `npx jest src/__tests__/coverage-boost.test.ts src/lib/auth/__tests__/password.test.ts src/modules/financeiro/lib/__tests__/crypto.test.ts src/lib/api/__tests__/action-route.test.ts --runInBand --coverage` → **PASS 4 suites** + estendido 7 suites 56 PASS; `npx jest src/__tests__/coverage-boost.test.ts --runInBand` 19/19 PASS; full targeted 12 suites **69/69 PASS** (campaign-branch + clinics + followup + outbox + waitlist + runtime-env).

- `collectCoverageFrom` mantido `['src/**/*.ts','!src/**/*.d.ts','!src/**/__tests__/**','!src/**/*.tsx']`; exclusão `!src/repositories/**` **não aplicada** — ADR `docs/adr/adr-coverage-repositories.md` permanece fallback apenas se CI ainda <70% após T1. Threshold não reduzido.

## Artefatos
- `coverage/lcov.info` (405089 bytes, 2026-08-26) + `coverage/lcov-report/index.html` — gerados por `npx jest --runInBand --coverage` full 245.3s.
- `npx jest src/__tests__/coverage-boost.test.ts --coverage` para smoke + CI Linux replica full.

## Conclusão W3/F3.14 — ainda FAIL global, gap quantificado

Global 63.26% ainda <70% Statements (falta 6.74pp ≈1100 stmts). Maior delta são `src/repositories/**` 0% (budgets 521 + treatment-plans 401 + waitlist 434 + campaigns 73% gaps etc = ~1800 lines) + `src/services/followup/inactive-patient` 18% + `src/lib/idempotency` 0%. Deltas T1 elevaram `src/lib/validations`→100%, `src/lib/errors`→100%, `src/lib/retry`→96.42%, `src/lib/logger`→69.56%, `campaign.service`→59% com F7.06 fechado, clinics→100% — mas impacto global limitado a +0.15pp porque repos pesam.

**Próximos passos (sem reduzir threshold):**
1. CI Linux replica medirá gap residual; se ainda 68-69%, aplicar ADR `!src/repositories/**` (justificativa: repos são integração via `npm run test:integration:run` loopback `synkroo_test`, já provado clinics 100% unit como padrão) → eleva global ~7-10pp para ≥70% sem inflar mock.
2. Ou adicionar integração focada para budgets/patients (não unit mock) — fora escopo T1.

## Próximos passos para 10/10
- `npx jest --runInBand --coverage` já verde local (286 suites) mas threshold falha por repos; ADR fallback documentado; `npm run typecheck`/`lint --quiet` nos arquivos tocados PASS (touch: eslint 3 files 0 warnings); `coverage/lcov.info` fresh; ledger permanece VERIFIED 126 até `roadmap:write` promover após W3 PASS.
