# F3.02 — Runtime-specific environment schemas

`parseRuntimeEnv` now validates app, bridge, agent and sidecar contracts. Errors contain only runtime and field names, never values. App accepts either `DATABASE_URL` or a Hyperdrive binding; worker schemas validate binding presence; sidecar requires explicit `SIDECAR_DEFAULT_OFF=true`.

| Verificação | Resultado |
|---|---|
| `npx jest src/lib/__tests__/runtime-env.test.ts --runInBand` | PASS — 3 testes |
| `npx tsc --noEmit --pretty false` | PASS |
| ESLint nos arquivos alterados | PASS |
| `git diff --check` | PASS |

Residual: wiring these schemas into each deployment bootstrap remains a separate worker/deploy gate.
