# F3.15 — CI PostgreSQL 17

O serviço `ci.postgres` em `.github/workflows/ci.yml` usa `pgvector/pgvector:pg17` e mantém `POSTGRES_DB: synkroo_test`. Nenhuma execução remota do GitHub Actions foi realizada.

| Verificação | Resultado |
|---|---|
| Assert textual de imagem e isolamento | PASS |
| `pgvector/pgvector:pg17` + `POSTGRES_DB: synkroo_test` | PASS |
| `npm run test:security` / `test:release` / `npm audit --omit=dev --audit-level=high` | PASS — workflow contém security + prod audit stage |
| `cf-build` `npm run build:cf` + `wrangler deploy --dry-run` | PASS |
| `git diff --check` | PASS |
| Orca parallel 2026-08-23 `ctx_da76` revalidado | PASS — YAML lint + tsc 0 |

Execução remota em GitHub Actions ainda não realizada — mantém `PARTIAL` até artifact de run existir (gate `W3`).
