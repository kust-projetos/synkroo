# F3.10 — Jest/jsdom major alignment

- `jest` remains `29.7.0`.
- `jest-environment-jsdom` was aligned from major 30 to `^29.7.0`.
- `package-lock.json` was regenerated and local node_modules installed with the aligned major.

| Verificação | Resultado |
|---|---|
| `npm ls jest jest-environment-jsdom --depth=0` | PASS — both `29.7.0` |
| Focused Jest DOM-compatible suite | PASS — 8 tests |
| `npx tsc --noEmit --pretty false` | PASS |
| `git diff --check` | PASS |
