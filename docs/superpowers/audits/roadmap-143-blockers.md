# Roadmap 143 Blocker Registry

Updated: 2026-08-20
Source of status: `docs/superpowers/audits/roadmap-143-ledger.json`
Execution contract: `docs/superpowers/plans/2026-08-16-roadmap-143-master-implementation.md`

This registry contains sanitized gates only. It never stores secret values, provider tokens, real pilot data or unapproved commands. A blocker pauses only its owning goal; independent READY work continues.

| Key | Class | IDs / goal | Reason and attempts | Preparation completed | Minimal human action | Preconditions | Risk | Rollback | Sanitized receipt | Resume | Independent READY alternatives |
|---|---|---|---|---|---|---|---|---|---|---|---|
| B-EMAIL-NORMALIZATION | R2 | F3.01 / O1-G07 | Duplicate/normalization preflight is not approved for a destructive or data-affecting migration. | Local schema/index inspection, isolated runner and preflight contract are planned; no production mutation. | Approve a maintenance window and choose duplicate disposition. | Isolated backup, duplicate export without PII, `DATABASE_URL` explicitly bound to approved test/staging DB. | Wrong merge or duplicate loss can corrupt tenant identity. | Restore backup; roll forward with an approved mapping; never destructive down-migrate automatically. | Duplicate counts/fingerprints, migration SHA, target DB class, timestamp, owner decision; no rows or credentials. | Re-run preflight, attach receipt, then activate O1-G07. | O1-G01–O1-G05 local security work. |
| B-HYPERDRIVE-STAGING | R4 | F2.14, F3.17 / O1-G06, O1-G11 | Local workerd/Wrangler `dbHealth` and 8-way concurrency are green, but deployed Cloudflare smoke is external. | Binding contract, dry-run config, local worker smoke and rollback packet are ready. | Authorize one staging deploy/invocation in the configured Cloudflare environment. | Rotated/scoped binding access, approved window, backup, smoke URL and rollback owner. | Provider/runtime misbinding or stale pool behavior could fail closed or leak dependency details. | Remove staging deployment or redeploy last compatible worker; no production change. | Worker version, request IDs/status classes, 8-way result, alert/rollback result, no secret value. | Mark receipt on F2.14/F3.17, rerun worker gate, then continue O1-G11. | O1-G02, O1-G03, O1-G05 and local O2 preparation. |
| B-GLOBAL-COVERAGE | R1 | F3.14 / O1-G10 | Latest recorded global coverage is 54.84% statements, 40.45% branches, 56.06% lines and 42.96% functions versus 70%; two local focused suites do not close global coverage. | `npm run verify` runner and focused validation/treatment tests exist; no threshold/exclusion/skip was weakened. | None; agent adds targeted coverage until all global thresholds pass. | Reproducible test command and coverage artifacts; no threshold edit. | False green release if global untested paths remain. | Revert only the coverage test change; keep the 70% gate. | Commit, command, four coverage percentages, threshold and changed test paths. | Add the next bounded coverage goal, run `npm run verify`, update ledger only after green. | O1-G01–O1-G09 and independent domain goals. |
| B-AUTH-MUTATION | R1 | F2.11 / O1-G05 | Repository target is 70.16%, but auth file retains 33 survivors and 1 no-coverage; password/session route behavior is green but exact F2.11 remains PARTIAL. | Transaction, lock, password change, pre-auth rate limit, focused unit/PostgreSQL double-run and LSP are green; residuals are documented. | None; agent adds assertions or narrows a proven unreachable branch without reducing threshold. | Mutation report, focused target, no new Stryker exclusion. | Residual auth mutation may hide session/revocation regressions. | Revert the assertion-only commit; keep previous passing cluster commit and threshold. | Stryker score, survivor count, test command, commit and reviewer finding; no report secrets. | Create next auth mutation goal, rerun target and update F2.11 only when item wording is complete. | O1-G01–O1-G04, O1-G06–O1-G11. |
| B-FINANCE-RACES | R1 | F9.01–F9.09 / O4-G04–O4-G05 | Asaas mappers/gateway update and PostgreSQL settlement/cancel/partial-failure races are not closed by local evidence yet. | Finance boundary and race-test design are in O4; no provider call or money movement was attempted. | None for local tests; owner later authorizes sandbox matrix if required. | Isolated DB, deterministic fixtures, provider contract doubles and explicit idempotency keys. | Duplicate charge, incorrect settlement or cancellation can create financial loss. | Transaction rollback/outbox reconciliation; never replay a provider mutation without idempotency key. | Sanitized event IDs, state transitions, race result, provider sandbox receipt and rollback result. | Start O4-G04 mappers, then O4-G05 race tests; pause only at provider gate. | O1/O2/O3 goals independent of finance. |
| B-CI-REMOTE | R3 | F3.15 / O1-G10 | Local CI workflow/config contracts can be checked, but remote GitHub Actions receipt has not been captured in this wave. | Workflow ordering, local commands and sanitized CI contract are prepared. | Run or authorize the configured CI workflow/PR check. | GitHub access already configured, branch SHA, environment approval and no secret rotation required. | Merged code may diverge from local toolchain or fail remote workers. | Re-run on same SHA or revert the PR; do not rewrite history. | Workflow URL/SHA, job statuses, artifact names and timestamps, no tokens. | Attach CI receipt to F3.15 and rerun Gate 1. | Local O1/O2 work not dependent on remote receipt. |
| B-PROVIDER-SANDBOX | R3 | F2.13, F6.02, F9.01–F9.09 / O3/O4 provider goals | Evolution, LLM and Asaas sandbox calls require configured provider access and bounded cost/tenant credentials. | Adapters, contract tests, timeout/failure cases and gate packets are prepared. | Authorize a sandbox smoke/matrix with a bounded cost and test tenant. | Provider sandbox, consent/test tenant, idempotency keys, timeout and cleanup plan. | External side effects, cost or data leakage. | Provider-side cancellation/cleanup plus local outbox reconciliation; no production call. | Provider name/version, request correlation IDs, status classes, cost class and cleanup result. | Update owning provider goal and rerun contract gate. | Local adapter, safety eval and consent work. |
| B-SECRET-ROTATION | R4 | F0.04–F0.10, F1.01 / O0-Gate-R | Secret rotation, fork/log/cache audit, history sanitation and credential invalidation require owner/provider authority. | Inventory, fingerprints, gitleaks staged/CI plan and no-value handling are ready. | Owner rotates affected credentials and confirms invalidation/audit window. | Named owner, provider consoles, backup/rollback communication plan and new credential delivery out of band. | Account takeover or secret exposure remains unresolved. | Provider-specific rollback only under owner authority; do not copy old values into repo/logs. | Secret class/fingerprint, owner, provider, timestamp, rotation result and clone invalidation status. | Attach receipt, rerun sanitized scans and reopen O0 external items. | All local code/tests and non-secret docs. |
| B-MIGRATION-APPLY | R4 | F3.04–F3.07 / O1-G07 | Duplicate cleanup, extension/index migration and staging apply require approved DB target; runbook must bind `DATABASE_URL` explicitly. | SQL/preflight, expand/contract order, backup and rollback packet are prepared; no unapproved apply. | Owner approves target and maintenance window for staging migration. | Backup verified, duplicate report, explicit `DATABASE_URL`, compatible app version and rollback owner. | Data loss, lockout or migration against wrong database. | Restore/roll forward using compatible schema; no destructive down migration. | Target class, migration journal, backup fingerprint, pre/post catalog counts and rollback result. | Correct runbook, apply in approved environment, rerun catalog/preflight. | Local schema tests, ledger and documentation. |
| B-PILOT-RESOURCES | R4 | F12.01–F12.07 / O5-G07–O5-G09 | Pilot tenant, approved dataset, participants, training and maintenance window are not authorized. | Dry-run onboarding/import/offboarding scripts, charter and rollback packet are planned. | Owner names pilot, approves tenant/dataset/team/window and authorizes apply. | Approved/anonymized dataset checksum, support contacts, backup and explicit import receipt. | Real data exposure, operational disruption or untracked scope change. | Offboard export/revoke/retention procedure and import-batch rollback. | Tenant alias, dataset checksum/counts, script version, window, operator and sanitized result. | Execute provisioning, then J-01–J-12 only after receipts. | O5 local release/observability work. |
| B-OUTAGE-DRILLS | R4 | F12.04 / O5-G08 | Controlled Evolution/LLM/DB/Queue/sidecar outage drills affect external resources and are not approved. | Fault-injection matrix, expected fail-closed behavior, alerts and runbooks are prepared. | Owner authorizes each bounded drill and recovery window. | Staging candidate, monitoring, rollback, participant notification and data-integrity check. | Lost messages, duplicate effects or prolonged degraded service. | Restore provider/queue/db dependency, drain/reconcile DLQ and rollback candidate. | Dependency, start/end, alert, mitigation, recovery time and integrity result. | Re-run failed journey/drill and update scorecard; never mark pass from simulation alone. | Local fault tests, alert/runbook review and release checks. |
| B-OWNER-GO-NO-GO | R5 | F12.08 / O5-G09 | Formal GO/NO-GO and accepted residual risk require owner decision after scorecard; agent has no decision authority. | Rubric, hard gates, scorecard fields and rollback conditions are defined. | Owner records GO or NO-GO for a named candidate SHA. | All 143 ledger receipts, score >=90, hard gates, pilot scorecard and residual-risk acceptance. | Premature release or unowned residual risk. | NO-GO keeps release blocked; rollback to last candidate remains available. | Decision, owner, date, candidate SHA, score, hard-gate list and accepted risks. | If GO, follow release gate; if NO-GO, open owning goals and preserve READY work. | Every independent local goal until final gate. |

## Registry rules

- R1/R2 blockers are resolved locally or environmentally before escalation.
- R3 runs only with configured authorization; capture a sanitized external receipt.
- R4/R5 stop only the owning dependency branch. Never pause independent READY nodes.
- After two identical failures, stop repeating, record root cause and try one orthogonal approach.
- A blocker is not a status promotion. The ledger is authoritative for item status.
