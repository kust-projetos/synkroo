# F3.13 — Triage de dependências

Comando reproduzível: `npm audit --omit=dev --json`.

## Resultado atual

| Pacote | Severidade | Origem | Ação | Status |
|---|---|---|---|---|
| `dompurify@3.4.12` | moderate | transitiva via `jspdf@4.2.1` | atualizar `jspdf`/`dompurify` quando release compatível estiver disponível; manter PDF redacted e sem HTML não confiável | WAIVER owner até 2026-09-15 |
| `esbuild@0.27.7` | low | transitiva via cadeia `tsx`/Tailwind | manter servidor de desenvolvimento apenas local; atualizar cadeia no próximo refresh de dependências | WAIVER owner até 2026-09-15 |

Nenhum finding high ou critical foi retornado no escopo de produção desta execução. O waiver não autoriza ignorar novos findings: CI deve continuar executando `npm audit --omit=dev --audit-level=high` e o owner deve revisar os dois itens antes da expiração.

## Evidência sanitizada

- Findings totais: 2 — 1 moderate, 1 low.
- Nenhum valor de segredo, URL privada ou credencial foi registrado.
- `npm ls dompurify esbuild --all --omit=dev` confirmou as cadeias transitivas acima.
