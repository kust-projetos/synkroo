# Veredito W3 / F3.14 — Pendente CI Linux 2026-08-25

> Gate W3: `jest.config.js:41` thresholds 70% branches/functions/lines/statements. `F3.14 global 63.11%→70%` é o único gate local bloqueando W3.

## Veredito: **PENDENTE CI** (não FAIL, não PASS local)

**7) W3/F3.14 = 7.5/10 — evidência local parcial, prova global exige runner Linux.**

### Evidência local (PASS)
- Baseline 2026-08-24: `Statements 63.11% (10263/16262) | Lines 64.19% | Branches 48.74% | Functions 55.20%` — FAIL threshold (sem lcov.info artefato).
- Delta 2026-08-25 (7 suites alvo, sem mock trivial inflado):
  - `src/__tests__/coverage-boost.test.ts` 13/13 PASS (retry/CircuitBreaker/tryCatch/handleApiError/logger redaction) — isola `src/lib/retry.ts`, `errors.ts:handleApiError`, `logger.ts` redaction
  - `src/services/followup/__tests__/inactive-patient.test.ts` 4/4 PASS (null/5d/0d, buckets 30/60/90/180)
  - `src/repositories/clinics/__tests__/clinics.test.ts` 3/3 PASS (found/null/delegate, 45 lines) — fecha `src/repositories/clinics` 0%→100%
  - `src/services/followup/__tests__/consent-guard.test.ts` 4/4 PASS (F7.08 versionado)
  - `src/modules/financeiro/services/__tests__/charge-race.test.ts` 2/2 PASS (F9.08 Promise.all idempotente)
  - `src/lib/ui/__tests__/sidebar-manifest.test.ts` 3/3 PASS (RBAC filterMenuByAccess)
  - `src/services/waitlist/__tests__/waitlist.fill.test.ts` 6/6 PASS (F5.04 idempotente)
  - `src/lib/outbox/__tests__/dispatch-dlq.test.ts` 3/3 PASS (F7.07 retry<5 / ≥5 dead_letter)
  - `src/lib/__tests__/runtime-env.test.ts` 4/4 PASS (F6.13 sidecar fail-closed)
  - **Targeted --coverage 38/38 PASS em 22.6s** (8 suites) + `npm run test:release` **13/13 PASS** + `typecheck:ia-bridge` **EXIT 0** + `typecheck:ia-agent` **EXIT 0** + `tsc --noEmit` previamente **EXIT 0**
  - `npm run roadmap:write` **EXIT 0** → `143 unique 126 VERIFIED /14 EXTERNAL /3 DEFERRED` (ledger não sobe sem W3 PASS)
  - `npx wrangler deploy --dry-run` bridge **EXIT 0** (6526 KiB) + agent **EXIT 0** (140 KiB) — W6 smoke local
  - `e2e/journey-patient.spec.ts` criado (lista→detalhe→create→edit→dedup + waitlist smoke) — W5 E2E
  - `collectCoverageFrom` mantido `['src/**/*.ts','!src/**/*.d.ts','!src/**/__tests__/**','!src/**/*.tsx']` — `!src/repositories/**` **não aplicado**, threshold **não reduzido**

### Por que PENDENTE (não PASS local)
- `npm test -- --runInBand --coverage` full **timeout 300s Windows** (277 suites, 16262 stmts) — não gera `coverage/lcov.info` nem `All files Statements ≥70%` neste runner. Targeted 6 suites `--coverage` já consome 53s e reporta `3.7%` global (threshold falha por escopo limitado, não por código).
- `npm run verify` full (`lint→typecheck→typecheck:bridge/agent→coverage→test:release`) também timeout 300s (lint+typecheck sozinhos >120s cada no Windows).
- Owner aprovou **CI Linux** (Ubuntu 22.04 / Node 22 / timeout 300s) em 2026-08-25 ("pode fazer o ci linux") — ver `f3-14-ci-linux-approval.md` (quando gateway voltar). Até CI gerar `coverage/lcov.info` com `All files 70.x%`, W3 não pode ser VERIFIED 8.5+ nem ledger 126→~140.

### Próximos passos para PASS (owner já aprovou CI)
```bash
# CI Linux
npm test -- --runInBand --coverage
# Expected: Statements 70.x% Lines 70.x% Branches 70% Functions 70% — PASS
# Artefatos: coverage/lcov.info + coverage/lcov-report/index.html
npm run roadmap:write && node scripts/roadmap-ledger.mjs --check
npm run verify  # lint --max-warnings=0 + typecheck + coverage + test:release → all gates passed
```
- Se ainda <70% após delta acima: ADR `!src/repositories/**` com justificativa (budgets 521 lines 0% é integração via `npm run test:integration:run`, já provado PG) — **sem reduzir 70%**, apenas isolate repos do coletor, como plano T1 Step 5 prevê. Não é fallback automático; é decisão owner-documentada.

### Risco
- Baixo para CI: deltas cobrem `lib/retry`, `errors`, `logger`, `inactive-patient`, `clinics` (45 lines) + `campaign-execution` 100% falha já coberto. Resta `budgets` 521 lines (0%) como único delta grande — se CI ainda 68-69%, ADR resolve sem inflar mock.

*Veredito 2026-08-25 — rubrica 7.5/10 (T1 7.5) em `docs/superpowers/plans/2026-08-25-pendencias-restantes-fechamento.md:423` e `rubrica-fechamento-2026-08-25.md`.*
