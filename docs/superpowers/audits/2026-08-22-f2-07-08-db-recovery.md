# F2.07–F2.08 — local database recovery receipt

Date: 2026-08-22

## Authorized local remediation

The read-only preflight found one duplicate normalized clinic/email group with two active rows in one clinic, no deleted rows. The user authorized the policy **keep oldest**.

A local transaction retained the oldest row and quarantined the newer row using a generated `@invalid.local` address, `is_active=false`, and `deleted_at=now()`. No email, user identity, or other PII was emitted in the receipt. No staging or production data was touched.

## Migration

- Docker Desktop was restarted successfully after the Linux Engine HTTP 500 blocker.
- `docker compose up -d --wait` reached a healthy `synkroo-db` container.
- `npm run db:health`: passed.
- `npm run db:migrate`: applied successfully.
- Read-only verification: `users_clinic_email_uniq` is present; duplicate group count is zero.

## Integration

- `users-email-unique.integration.test.ts`: 2/2 passed.
- Full `npm run test:integration -- --testTimeout=30000`: 36 suites, 206 tests passed.
- `jest.integration.config.js` now loads `.env.local` quietly so the standard integration command receives the local database URL.
- `seed-test-clinic.mjs` now reconciles an existing clinic slug and reuses the oldest existing demo user identity, preserving idempotency under the new unique constraint.

Commits:

- `2095c71c` — load integration env and reconcile demo clinic slug.
- `14fec1c7` — reuse existing demo user identity.

This receipt applies only to the local test database. Staging/production migration remains a separate authorized gate.
