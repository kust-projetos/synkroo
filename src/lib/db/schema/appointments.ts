// ──────────────────────────────────────────────
// [MIGRATED] Schema moved to @/modules/operacional/schema
// Legacy re-export for backward compatibility
// ──────────────────────────────────────────────
export {
  appointments,
  scheduleBlocks,
  appointmentReminders,
  waitlist,
  appointmentReminderConfigs,
  procedureTypes,
} from '../../../modules/operacional/schema/appointments';
